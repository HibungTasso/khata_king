import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.indigo;
  static const primaryLight = Color(0xFFE8EAF6);

  static const incomeColor = Colors.green;
  static const incomeBg = Color(0xFFDFF5E1);

  static const expenseColor = Colors.red;
  static const expenseBg = Color(0xFFF8D7DA);

  static const textDark = Colors.black87;
  static const textLight = Colors.white;
}
